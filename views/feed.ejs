<%- include('partials/header') -%>
  <div class="container mx-auto">
    <div class="row justify-center mt-5">
      <ul class="flex flex-row flex-wrap list-unstyled">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="basis-1/3 pt-5 pb-5 h-64">
            <!-- 
            <h2>
              Name: <%= posts[i].traitName %>
            </h2>
            <span>Description:</span>
            <p>
              <%= posts[i].caption %>
            </p>
            <span>Category:</span>
            <p>
              <%= posts[i].category%>
            </p>
            <span>Deck:</span>
            <p><%= posts[i].deck%>
            </p>
            <a href="/post/<%= posts[i]._id%>">
              <div class="badge badge-outline">View</div>
            </a> -->

            <div class="card w-96 bg-base-100 shadow-xl h-64">
              <div class="card-body flex justify-between">
                <div class="tooltip" data-tip="Name">
                  <h2 class="card-title">
                    <%= posts[i].traitName %>
                  </h2>
                </div>
                <div class="tooltip" data-tip="Description">
                  <p>
                    <%= posts[i].caption %>
                  </p>
                </div>
                <div class="card-actions justify-around">
                  <div class="tooltip" data-tip="Deck">
                    <button class="btn btn-secondary">
                      <%= posts[i].deck%>
                    </button>
                  </div>
                  <div class="tooltip" data-tip="Category">
                    <button class="btn btn-primary">
                      <%= posts[i].category%>
                    </button>
                  </div>
                </div>
              </div>
            </div>

          </li>
          <% } %>
      </ul>
    </div>
  </div>
  <%- include('partials/footer') -%>